//Insert & remove element from array.
//array.push(element) will be add on last position of array
//array.unshift(element) will be add on first position on an array.


//array.splice(index, which index of element you wanted to remove or deleteCount, which index of element you wanted to add...etc how much you want to add).

// const array = [1, 2, 3, 4, 6];

// array.splice(2, 0, 9, 'hello')
// console.log(array)

// to remove last element of array use array.pop()
// to remove first element of array use array.shift();

/* to use specific element of an array use array.splice() method
const array = [1, 2, 3, 4, 6];

array.splice(2, 1)
console.log(array)*/