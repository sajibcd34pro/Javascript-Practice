var name = '';
var fname = name || 'Md Sajib';
console.log(fname);

// 
var isOk = true;

// var isOkResult = isOk ? 'EveryThing is Ok' : 'not ok';
var isOkResult = isOk && 'EveryThing is Ok';

console.log(isOkResult);